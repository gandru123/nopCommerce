apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: nop-ingress

spec:
  ingressClassName: traefik

  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: nop-commerce-service
            port:
              number: 80